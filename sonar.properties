# Required metadata
sonar.projectKey=intern-feedback-helloworld
sonar.projectName=Intern feedback helloworld
sonar.projectVersion=0.1

# Comma-separated paths to directories with sources (required)
sonar.sources=src

# exclude some files and folders (typically dependencies)
sonar.exclusions=src/vendor/**/*, node_modules/**/*, build/**/*, test/**/*, test-karma/**/*

# Encoding of the source files
sonar.sourceEncoding=UTF-8